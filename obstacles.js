class Obstacles {
  constructor(width, height, radius, position) {
    this.width = width;
    this.height = height;
    this.radius = radius;
    this.position = position;
  }

  createObstacles() {}
}
